# Provider Configurations
# Note: required_providers is defined in main.tf, so not repeating here

# Terraform configuration is in main.tf
# This file can contain additional provider configurations if needed

# Example: Additional provider configurations can go here
# provider "kubernetes" {
#   # Additional kubernetes provider config if needed
# }
